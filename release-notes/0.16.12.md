# MachBoost 0.16.12

## Fixed

- Detect Gemma 4's native pipe-delimited tool protocol in downloaded chat
  templates, so compatible models expose Tools and can enter Dev mode.
- Parse single and parallel Gemma 4 function calls into the existing OpenAI and
  Ollama-compatible response schemas without leaking protocol tokens into chat.
- Preserve nested objects, arrays, booleans, numbers, and delimiter-escaped
  strings in Gemma tool arguments.

## Verified

- The cached `gemma-4-26B-A4B-it-QAT-MLX-4bit` model generated a structured
  `list_files` call, accepted its tool result, and returned a final answer.
- The warm app-compatible `/api/chat` tool request completed in 0.60 seconds in
  the release test environment.
- All 595 Python tests pass.
