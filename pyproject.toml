[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "machboost"
version = "0.3.0"
description = "Resident local inference server with adaptive context-backed acceleration for MLX and Hugging Face."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name = "Vistrit Pandey", email = "vistrit@blinkfire.com" }
]
keywords = ["llm", "inference", "speculative-decoding", "local-ai", "mlx", "huggingface", "ollama", "server"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: MacOS",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = []

[project.optional-dependencies]
hf = [
  "torch",
  "transformers",
]
mlx = [
  "mlx",
  "mlx-lm",
]
vision = [
  "mlx-vlm>=0.5.0",
]
all = [
  "torch",
  "transformers",
  "mlx",
  "mlx-lm",
  "mlx-vlm>=0.5.0",
]

[project.scripts]
machboost = "machboost.cli:main"

[project.urls]
Homepage = "https://github.com/VistritPandey/machboost"
Repository = "https://github.com/VistritPandey/machboost"
Issues = "https://github.com/VistritPandey/machboost/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["machboost*"]
